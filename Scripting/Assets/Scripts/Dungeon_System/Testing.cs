using Code_DungeonSystem;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testing : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*PathFinding path = new PathFinding(20, 20);

        List<CellTesting> cells = path.FingPath(0, 0, 19, 19);

        if (cells != null)
        {
            Debug.Log(cells[cells.Count - 1]);
        }
        else
        {
            Debug.Log("XD");
        }*/

            


        
    }
}
